import type { Viewport } from "next";

const viewport: Viewport = {
  themeColor: "#2563eb",
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  colorScheme: "light",
  viewportFit: "cover",
};

export default viewport;
